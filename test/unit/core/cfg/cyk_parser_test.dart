import 'package:flutter_test/flutter_test.dart';

// Placeholder failing tests for Phase 3.2 T017 (CYK parser trees).

void main() {
  group('CYK parser', () {
    test('Produces derivation tree for string in language', () {
      expect(false, isTrue, reason: 'Pending CYK parser implementation');
    });
  });
}
